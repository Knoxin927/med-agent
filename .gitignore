# 忽略存放本机真实密码的环境变量文件，避免敏感信息被 Git 提交。
.env
# 原始知识库资料只保留在本机；允许空目录占位文件入库。
data/raw/*
!data/raw/.gitkeep
# Chroma 会在这里写入向量、原文和索引文件，属于本机派生数据，不能提交。
data/chroma/*
!data/chroma/.gitkeep
# M1.2 的独立虚拟环境只服务本机开发，不进入 Git。
.venv-m1-2/
# M6.1 公开候选临时导出目录：只在本机构建候选导出产物，绝不能进入 Git。
.m6-public-candidate/
.m6-public-candidate-staging-*/
.m6-public-candidate-backup-*/
# 会话审计/关闭门产生的临时日志与退出码文件，不入库。
*.out
*.code
*-log.txt
*-logdir.txt
*-record-path.txt
*-py.txt
*-run.txt
tmp_*
*.b64
# 本机临时语料与评测 stderr，不入库。
evaluation/corpora/v2-smoke/
evaluation/**/_*.out
evaluation/**/_*.txt
