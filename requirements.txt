# 固定 FastAPI 的直接版本，减少容器构建时框架版本漂移。
fastapi==0.115.12
# 固定 Uvicorn 的直接版本，它负责运行 FastAPI 的 ASGI 应用。
uvicorn==0.34.2
# 固定本地 Embedding 模型运行库，用于加载 BAAI/bge-m3 并生成 dense 向量。
sentence-transformers==4.1.0
# 固定本地向量数据库客户端，用于持久化保存文本、向量和 metadata。
chromadb==0.6.3
# 固定 Chroma 0.6.3 兼容的遥测客户端版本，避免新版 capture 接口产生错误日志。
posthog==5.4.0
# 固定异步 HTTP 客户端，用于读取 OpenAI-compatible 上游的流式响应。
httpx==0.28.1
# 固定 dotenv 读取库，让直接运行 Uvicorn 时也能从未跟踪的 .env 加载配置。
python-dotenv==1.1.0
# 固定 MIT 许可的 LangGraph 编排框架，M3.3 仅用其 StateGraph 调度项目自有的 AgentState。
langgraph==1.2.10
# 固定 PostgreSQL 驱动，M3.4 用参数化 SQL 持久化 Agent run 的 CAS 版本事实。
psycopg[binary]==3.3.4
# 固定 MCP Python SDK 1.29.0（MIT，Python>=3.10），只用于本机 stdio 协议适配。
# 故意不选 2.0.0，避免未验证的跨大版本 API 变化进入 M4.1 最小闭环。
mcp==1.29.0
# 固定 sse-starlette 2.x：mcp 会拉到更新版本，但其要求的 starlette>=0.49 与
# 当前 FastAPI 0.115.12 的 starlette<0.47 冲突；2.1.3 可与现有 FastAPI 共存。
sse-starlette==2.1.3
